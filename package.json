{
  "name": "glsl-shaders",
  "version": "1.0.0",
  "description": "GLSL shader portfolio",
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && node server.js",
    "dev":   "npm run build && DEV=1 node server.js"
  },
  "dependencies": {}
}
