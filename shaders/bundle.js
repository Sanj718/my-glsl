// Auto-generated by build.js — do not edit
export const SHADERS = [
  { name: "Bg Color", frag: "#ifdef GL_ES\nprecision mediump float;\n#endif\n\n#define PI 3.14159265359\n\nuniform float uTime;\nuniform vec2 uResolution;\n// \nvoid main() {\n    vec2 q = gl_FragCoord.xy / uResolution.xy;\n\n    // 1. Create two different \"waves\" for X and Y\n    // We use different speeds (0.7 and 1.3) so they don't sync up perfectly\n    float waveA = sin(q.x * 3.0 + uTime * 0.7 + q.y);\n    float waveB = cos(q.y * 2.0 + uTime * 1.3 + q.x);\n\n    // Combine them to get a \"blobby\" value between 0.0 and 1.0\n    float combined = (waveA * waveB) * 0.5 + 0.5;\n\n    // 2. Define your palette from the image\n    vec3 cottonCandy = vec3(0.89, 0.58, 0.62);\n    vec3 teaGreen = vec3(0.76, 0.88, 0.68);\n    vec3 evergreen = vec3(0.06, 0.13, 0.17);\n\n    // 3. The \"Double Mix\"\n    // First, blend the two light colors together based on horizontal position\n    vec3 highlightColor = mix(cottonCandy, teaGreen, q.x);\n\n    // Then, use our \"combined\" wave to decide where the dark evergreen shows up\n    // smoothstep(0.2, 0.8, combined) makes the edges of the \"blobs\" softer\n    vec3 finalRGB = mix(evergreen, highlightColor, smoothstep(0.2, 0.8, combined));\n\n    gl_FragColor = vec4(finalRGB, 1.0);\n}" }
];
